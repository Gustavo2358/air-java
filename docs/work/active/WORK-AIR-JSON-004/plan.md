# Plan

1. Confirm clean origin/main baseline and archive merged W1B per lifecycle. Separate documentation-only commit `6b5fc6bdc22949d55418974f335a7735198867c8` precedes this work; official protocol allows next authorized maintenance without a new main commit. W2C baseline remains main `2a37f5e980ba25fdc79614a66030a84d8bf5b8c9`, tree `8d248f4ccf207eb7b609aa9ff0cdbcd512e526c8`.
2. Write independent model and binding oracles; preserve five REDs with exact gap paths. Mapping-level Branch probe prevents BOOL from masking Branch; after BOOL GREEN, repeat public Branch RED before adding Branch. Similarly isolate Unknown at its mapping and public API before adding it.
3. Extend only reader/writer mappings, no generated IDs, sorting arrays, inference or repair. Existing typed closure indices remain authoritative. List traversal O(serialized facts), auxiliary stacks O(depth); no destination/storage/reason inventory joins in codec.
4. GREEN, negative taxonomy, frozen W1 bytes, symmetric canonical cycles, independent-process exports and N/2N work counts. Run compilable mutations in disposable source copies, preserve logs/hashes, restore exactly and second GREEN.
5. fast/architecture/semantic/transport/full and git --work. Dedicated performance gate UNAVAILABLE; focal probes only. Self-review, staged diff, commit, push, Draft PR; exact-head remote CI including synthetic merge receipt. No merge or auto-merge.

Rollback is a new reviewed change; never discard preexisting work. Product changes stay separate from administrative closeout. Evidence files remain immutable once recorded; corrections receive new receipts.
