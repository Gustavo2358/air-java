# Plan

1. Confirm baseline and merged W1A; archive the previous merged baseline item and reserve JSON-003 from registry/history.
2. List binding closure before coding; public-model fixtures and independent wire expectations; preserve baseline coverage RED.
3. Isolate the admission RED with test-only mapping bypass, then add explicit mappings and repeat the policy RED with real mappings.
4. Change only transport admission; test literal/computed, open bounds, contracts and negative validation.
5. GREEN, compilable semantic mutations in disposable copies, byte-exact restoration and second GREEN; full canonical gates and scope.
6. Document exact W1C handoff, commit/push, Draft PR and verify remote HEAD/tree/CI. Stop for review.

Mappings traverse each mapped list once, without inventory lookups. Union scopes and expression recursion are outside this subset. Rollback is a normal reviewed revert, never reset/stash/discard. No runtime counters or cardinality caps.
