# Evaluation

Oracle: public AIR records independent of codec; explicit expected wire subtrees from binding §§7–10. Literal and computed Read(ObjectPlace), origin identity, all complete records, deterministic bytes and I-56 before/after. Closed zero arity and open remainder with no known positions; broad effects including mustOverwrite references; finite invocation alternatives and open control; Known/UnknownContract.

Negatives: malformed fields/types/duplicates/unknown kinds, reference/owner errors, INVALID_IR including truncated diagnostics, RESOURCE_LIMIT, UNSUPPORTED_CAPABILITY, non-obligation VALIDATION_LIMIT with mappings available, and unimplemented variants. Existing GOBACK/scalar goldens unchanged. N/2N Invoke probes without time thresholds.

Required mutations: literal name, computed expression, Read object, normal outcome, effects scope, contract uncertainty/evidence, invalid admission, generic incomplete admission, I-56 suppression and old GOBACK bytes. Compile failures do not count. Restore hashes and run second GREEN. Canonical full gate includes model/transport, architecture and Maven clean verify. Dedicated performance gate is UNAVAILABLE; report actual probes only.
